const statData = require('./statData');
const users = require('./users');
const chatData = require('./chatData');

module.exports = {
    statData: statData,
    users: users,
    chatData: chatData
};